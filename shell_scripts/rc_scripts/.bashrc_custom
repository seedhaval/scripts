### Custom bash script. Call this script from .bashrc like so
# . ~/.bashrc_custom

# Include bin scripts in path
export PATH=$PATH:~/scripts/bin/

# Use VI mode for editing
set -o vi

alias py3=python3
alias run_interface="python3 ~/scripts/shell_scripts/userland/interface.py"
