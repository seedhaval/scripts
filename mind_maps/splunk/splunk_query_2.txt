splunk query
    accum
        accumulated ( cumulative ) sum
        views as totalViews
    addcoltotals
        totals shown at bottom of table in statistics tab
        addcoltotals bytes duration
            list of columns
        addcoltotals *size
            show totals for column name that matches pattern
        if no column specified, does total for all numeric fields
    addinfo
        add internal info about search to events
        info_min_time
        info_max_time
        info_sid
        info_search_time
            time when search was run
    addtotals
        totals shown at end of row
        fieldname=sum
            output column name
        addtotals user* *size
            specify patterns for fields which need to be added
