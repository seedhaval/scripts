splunk dashboard
    input fields
        populate values from query
        specify earliest and latest
    preview
        on
            output updated as records become available
        off
            all output shown at once
    refresh
        manual
        set auto refresh interval
    token
        $tokabc$
    get query
        click on search icon on bottom right of panel
    drill down
        set tokens
    format on save
        XML
            old
        JSON
            new
