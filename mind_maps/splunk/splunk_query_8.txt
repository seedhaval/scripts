splunk query
    fieldsummary
        summary statistics
            count
            distinct_count
            is_exact
                whether distinct count is exact or approx
            max
            min
            mean
            numeric_count
                number of events containing numbers
                nulls are skipped
            stddev
            values
                distinct values and corresponding counts
            maxvals
                number of unique values to return
                default 100
                0 for no limit
            wildcards supported in field list    
