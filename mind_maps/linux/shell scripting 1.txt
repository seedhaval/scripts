shell scripting 1
   shebang
      #!/bin/bash
      #!/usr/bin/env python
   error handling
      set
         -e
            stop execution on error
         -u
            error on undefined variable
         -o pipefail
            fail if any command in the pipe fails
   debug
      set -x
         echo command before execution
      echo
         add new line
      printf
         do not add new line
   programming constructs
      if
         then
         elif
         else
         fi
   for var1 in *
      do
      done
   read
      user input
      -p
         specify a message to be prompted
