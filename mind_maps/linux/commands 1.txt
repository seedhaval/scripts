commands 1
   ls
      -l
         long listing
      -1
         only 1 file per line
         only show file name
      -t
         sort by time
      -r
         reverse order
      -a
         show hidden
            starting with .
      -R
         recursive
   cd
      -
         go to previous directory
      empty
         go to home directory
      ~
         go to home directory
      ..
         go to parent directory
   du
      show folder usage
      -s
         show summary
      -h
         show human format
            kb, mb, gb
   df
      show drive usage
      -h
         show human format
   cat
      show file contents
   tac
      show file in reverse order
