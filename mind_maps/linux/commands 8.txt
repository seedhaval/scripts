commands 8
   whoami
      get current user name
   man
      display manual page for a command
      vi commands can be used to navigate
   less
      pipe output of a large command to less
      vi commands can be used to navigate
   uniq
      remove duplicate lines from a sorted file
      only remove duplicates if lines are consecutive
      -c
         show count of duplicate lines against line data
   sort
      sort standard input
      -n
         sort as number
      -u
         remove duplicate
      -r
         sort in reverse
      -h
         human numeric sort
         2k, 1m, etc
   chmod
      change permission
      octal rwx, user, group, other
      +x
         add executable permission for owner
      755
         rwx user, rx for group, others
