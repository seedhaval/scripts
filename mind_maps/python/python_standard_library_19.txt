python standard library
    datetime
        datetime
            instance methods
                date()
                    return date object from datetime
                time()
                    return time object from datetime
                timetz()
                    return time object along with timezone info
                replace
                    replace one or more of 
                        year, month, day, hour, minute, second, microsecond
                        tzinfo, fold
                astimezone( newtz )
                    return datetime object with new timezone newtz
                    if newtz not provided, provide in current local timezone
                utcoffset
                    get utc offset of timezone if present
                tzname
                    get timezone name of self if present    
