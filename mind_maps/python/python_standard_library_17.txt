python standard library
    datetime
        date
            weekday
                day of week. Monday is 0
            isoweekday()
                day of week, Monday is 1
            isocalendar()
                year, week, weekday
            isoformat()
                return string - YYYY-MM-DD
            ctime()
                return string representing date
                hour, minute, second = 0
            strftime
                return string formatted as per provided format string
        datetime
            datetime( year, month, day, hour=0, minute=0, second=0, microsecond=0, tzinfo=None, fold=0 )
            today()
                get current local datetime
            now( tz = None )
                tz must be instance of tzinfo class
