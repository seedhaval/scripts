python standard library
    textwrap
        shorten
            parameters
                text, width, fix_sentence_endings=False
                break_long_words=True, break_on_hyphens=True
                placeholder=' [...]'
            collapse and truncate text to fit the given width
            all whitespace replaced by single space
        dedent( text )
            remove common leading whitespaces from all lines
        indent( text, prefix, predicate=None )
            add prefix to begining of lines
            if predicate is None, prefix is added to all lines except the ones which are only whitespace / empty
            predicate - function to decide which line prefix is to be added
                indent( s, '+', lambda line: True )
                    add prefix to all lines ( including empty / whitespace only )
        attribute description
            replace_whitespace
                after tab expansion but before wrapping
                replace all whitespace character with single space
            drop_whitespace
                drop whitespace at begining and ending of every line
                whitespace at begining of paragraph is not dropped
