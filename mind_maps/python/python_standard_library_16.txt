python standard library
    datetime
        date
            fromtimestamp
                return date corresponding to provided POSIX timestamp
            fromordinal
                return date corresponding to ordinal
                January 1 has ordinal 1
            fromisoformat
                date string in YYYY-MM-DD
            fromisocalendar( year, week, day )
            isntance attributes
                year
                month
                day
            replace( year=self.year, month=self.month, day=self.day )
                return new date with values replaced wherever provided
            timetuple
                return tuple of time
                hour, minute, second = 0
            toordinal
                convert to ordianal. January 1 is ordinal 1
